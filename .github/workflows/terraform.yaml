# name: 'Terraform'

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# permissions:
#   contents: read
#   pull-requests: write # Required if you want to post plan outputs to PRs

# defaults:
#   run:
#     shell: bash

# jobs:
#   terraform_plan:
#     name: 'Terraform Plan'
#     runs-on: ubuntu-latest

#     steps:
#     # Checkout the repository to the GitHub Actions runner
#     - name: Checkout
#       uses: actions/checkout@v4

#     # Install the latest version of Terraform CLI
#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

#     # Initialize Terraform working directory
#     - name: Terraform Init
#       run: terraform init

#     # Checks that all configuration files adhere to a canonical format
#     - name: Terraform Format
#       run: terraform fmt -check

#     # Generates an execution plan for Terraform
#     - name: Terraform Plan
#       run: terraform plan -input=false

#   terraform_apply:
#     name: 'Terraform Apply'
#     runs-on: ubuntu-latest
#     needs: terraform_plan
#     # Only run apply when code is successfully pushed/merged into the main branch
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     # This environment enforces the manual approval gate configured in GitHub
#     environment: production

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4

#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v3
#       with:
#         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

#     - name: Terraform Init
#       run: terraform init

#     # Optional but highly recommended: run plan again to ensure state consistency before applying
#     - name: Terraform Plan
#       run: terraform plan -input=false

#     # Build or change infrastructure
#     - name: Terraform Apply
#       run: terraform apply -auto-approve -input=false